import { withRouter } from "react-router-dom";
const ProfilePersonnel = () => {
    return ( <div>
        ici c'est le profile personnel d'un user 
    </div> );
}
 
export default withRouter(ProfilePersonnel);